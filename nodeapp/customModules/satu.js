const tambahDua = (num) => num + 2;
const tambahTiga = (num) => num + 3;

module.exports = { tambahDua, tambahTiga };

// module.exports = {
//   tambahDua: (num) => num + 2,
//   tambahTiga: (num) => num + 3,
// };
